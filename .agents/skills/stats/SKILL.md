---
name: stats
description: Report language-tutor project totals: generated stories, vocabulary entries, and tracked grammar constructions. Use when the learner asks for statistics, progress, totals, or counts.
---

# Tutor Stats

Run `python3 .claude/skills/stats/scripts/stats.py` and report the three totals concisely. Use `--json` only when the counts need to be passed to another tool. Do not ask the learner to run the command.
