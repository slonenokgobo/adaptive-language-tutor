---
name: stats
description: Report Portuguese tutor-project totals: generated stories, vocabulary entries, and tracked grammar constructions. Use when the learner asks for statistics, progress, totals, or counts.
---

# Tutor Stats

Run:

```bash
python3 .claude/skills/stats/scripts/stats.py
```

Report the three totals concisely. Use `--json` only when the counts need to be passed to another tool.
